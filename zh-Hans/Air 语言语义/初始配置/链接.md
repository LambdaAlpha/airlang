# 链接

## _link.new

类型：function

上下文访问：无关

创建一个链接，其值为 i。

## _link.new_constant

类型：function

上下文访问：无关

创建一个不可变链接，其值为 i。

## _link.which

类型：function

上下文访问：可变

输入求值：否

i 为 `link : _ f i`，对 f 求值，得到函数 f1，若 f1 需要对 i 求值，则对 i 求值，得到 i1，以 link 的值为上下文，以 i1 为输入调用 f1。
