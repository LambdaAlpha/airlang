# 语言

## .language.semantics.eval

类型：function

求值 input。

## .language.syntax.parse

类型：function

上下文：无关

input 应为字符串，将 input 以 Air 语法解析成一个值 v，返回 .(v)。

## .language.syntax.generate_pretty

类型：function

上下文：不变

输入：无关

返回 ctx 的 Air 语法表示文本。

## .language.syntax.generate_key

类型：function

上下文：不变

输入：无关

返回 ctx 的 Air 键表示，满足两个值相等等价于他们的键相等。

## .language.syntax.is_valid

类型：function

上下文：不变

输入：无关

返回 ctx 是否是一个语法值。

## .language.version

类型：list

语言的 `主版本号 : 次版本号 : 修订号`
