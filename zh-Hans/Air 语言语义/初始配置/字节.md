# 字节

## .byte.get_length

类型：function

上下文：不变

输入：无关

ctx 应为字节，输出 ctx 的长度。

## .byte.push

类型：function

上下文：可变

输入：相关

input 应为字节，ctx 应为字节，将 input 附加到 ctx 的后面。

## .byte.join

类型：function

上下文：无关

输入：相关

input 应为字节列表，将 input 拼接成一个字节。
