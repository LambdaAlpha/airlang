# 资源

## _resource.get_steps

类型：function

上下文访问：无关

获取当前配置可用步数。

## _resource.set_steps

类型：function

上下文访问：无关

若非负整数 i 小于当前配置的可用步数，则设置当前配置可用步数为 i。

## _resource.measure_steps

类型：function

上下文访问：可变

输入求值：否

对 i 进行求值，返回 `o : steps`，o 是求值的输出，steps 是求值的步数。
