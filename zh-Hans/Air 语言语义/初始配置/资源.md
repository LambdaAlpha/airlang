# 资源

## .resource.get_steps

类型：function

上下文：无关

输入：无关

获取当前配置可用步数。

## .resource.set_steps

类型：function

上下文：无关

输入：相关

若非负整数 i 小于当前配置的可用步数，则设置当前配置可用步数为 i。

## .resource.measure_steps

类型：function

上下文：可变

输入：相关

对 i 进行求值，得到 `o`，返回 `o : steps`，steps 是求值的步数。
