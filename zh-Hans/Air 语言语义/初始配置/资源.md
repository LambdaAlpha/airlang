# 资源

## _resource.available_steps

类型：function

上下文访问：无关

获取当前配置可用步数。

## _resource.measure_steps

类型：function

上下文访问：可变

输入求值：否

对 i 进行求值，返回 `o : steps`，o 是求值的输出，steps 是求值的步数。

## _resource.limit_steps

类型：function

上下文访问：可变

i 为 `steps : v`，对 v 进行求值并返回输出，限制求值步数为 `steps`，当步数用完时，强制停止求值，返回单元。
