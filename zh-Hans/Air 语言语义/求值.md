# 求值

## 上下文

### 单元格

- `.cell.value`：表示单元格中的值

### 配对

- `.pair.left`：表示配对的左元素
- `.pair.right`：表示配对的右元素

可读写

### 列表

- `.list.first`：表示列表的第一个元素
- `.list.last`：表示列表的最后一个元素
- 整数：表示列表第 `i` 个元素

可读写

### 映射

键 `k` 表示映射中 `k` 所映射的值

可读写，可新增，可移除

### 引用

- `.quote.value`：表示引用中的源

### 调用

- `.call.function`：表示调用的函数
- `.call.input`：表示调用的输入

可读写

### 求解

- `.solve.function`：表示求解的函数
- `.solve.output`：表示求解的输出

可读写

## 键

- `_a` ➔ `a`
- `.a` ➔ `.a`
- `a` ➔ `v`，`v` 为上下文中键 `a` 所绑定的值

## 引用

`_(v)` ➔ `v`

## 调用

`_ f i` ➔ `f'(i')`，其中 `x'` 表示 `x` 求值的结果，下同。

## 求解

`? f o` ➔ `i`，过程如下：

1. 对 `f` 和 `o` 求值，得到函数 `f'` 和输出 `o'`
2. 从配置中以 `.solver` 为键获取求解器函数
3. 以 `f' : o'` 为输入调用求解器，求解器须返回一条事实
4. 验证事实的函数和输出与 `f'`、`o'` 一致
5. 返回事实的输入 `i`

## 结构

- `.(v)` ➔ `.(v')`
- `v1 : v2` ➔ `v1' : v2'`
- `[v1, v2, ..., vn]` ➔ `[v1', v2', ..., vn']`
- `{k1 : v1, k2 : v2, ..., kn : vn}` ➔ `{k1 : v1', k2 : v2', ..., kn : vn'}`

## 其他

其他值的求值规则为 `v` ➔ `v`。
