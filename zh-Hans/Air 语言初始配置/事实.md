# 事实

## .fact.make

类型：function

input 应为 `func : input`，以 input 调用 func，构造一条事实，输出该事实。

## .fact.get_function

类型：function

上下文：无关

input 应为事实，返回 input 的函数。

## .fact.get_input

类型：function

上下文：无关

input 应为事实，返回 input 的输入。

## .fact.get_output

类型：function

上下文：无关

input 应为事实，返回 input 的输出。
