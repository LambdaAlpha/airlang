# Pair

## .pair.get_left

Type: function

Context: constant

Input: free

ctx should be a pair, outputs ctx's left value.

## .pair.set_left

Type: function

Context: mutable

ctx should be a pair, sets ctx's left value to input, outputs ctx's original left value.

## .pair.get_right

Type: function

Context: constant

Input: free

ctx should be a pair, outputs ctx's right value.

## .pair.set_right

Type: function

Context: mutable

ctx should be a pair, sets ctx's right value to input, outputs ctx's original right value.
