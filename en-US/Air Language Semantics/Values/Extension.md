# Extension

The extension type is an extension of the language's built-in types, defined by library users.

The extension type provides the ability to store and retrieve arbitrary values in configs and contexts, as well as the ability for functions to process or generate arbitrary values.
