# Config

The config type represents a configuration.

A config is keyed by keys; values can be read, added, or overwritten. After an overwrite ends, the original value is restored.

## Abort

Indicates whether the config's code execution has been aborted.
